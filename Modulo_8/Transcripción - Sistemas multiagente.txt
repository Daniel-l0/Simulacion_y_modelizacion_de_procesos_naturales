[MÚSICA] El módulo pasado, detallé lo que eran los agentes. Y ahora veremos cómo construiremos un sistema simplemente colocando varios agentes en el mismo entorno. Por lo general, hablamos de sistemas multiagente, y puede tener ese aspecto. Así que tenemos una colección de agentes. Al igual que antes, están en los entornos. Pero ahora los agentes pueden interactuar entre sí. Hay varias formas de entender y terminar esta interacción. Aquí, intento detallar la conexión, la interacción entre los agentes. Pero, de hecho, por supuesto un agente puede simplemente ver al otro agente como parte de su percepción del entorno. Así que, de nuevo, desde un agente, todo el resto puede verse como una caja negra. Esta propiedad es realmente interesante para el modelado porque significa que podemos centrarnos en un punto del modelo y considerar que todo el resto es una caja negra uniforme. Y así podemos refinar las reglas del agente sin preocuparnos del resto. Y con la esperanza de que haciendo eso y poniendo todo el agente [INAUDIBLE] dentro, juntos, se produzca un comportamiento emergente. Así que ahora hablaré un poco sobre cuáles son los aspectos que intervienen en el sistema multiagente. Así que, lo primero que tenemos que decir es que la mayoría de los sistemas basados en agentes, utilizan un sistema multiagente porque un solo agente es sólo una función. Así que es sólo un programa, no necesitamos ningún marco inteligente para hacerlo. Así que la mayoría, lo interesante de esto es el hecho de que vamos a poner varios modelos juntos, varios agentes. Y esperamos que por la interacción o el efecto acumulativo de todos estos agentes, suceda algo rico. Y aquí estamos discutiendo sobre simulación y modelación de procesos naturales y por supuesto sobre modelación basada en agentes. Pero preste atención porque los sistemas multiagente se utilizan, de hecho, en muchos otros campos como la optimización, por ejemplo. El hecho es que muchos métodos de optimización utilizan, agentes que intentarán encontrar la solución optimizada. No hablaré de ello. Existen métodos realmente interesantes de seguridad de redes en los que algunos agentes son móviles y saltarán de un ordenador a otro e intentarán monitorizar lo que está pasando. No hablaré de ello. Y, por último, los videojuegos son quizás el ejemplo más interesante del sistema que son multiagentes pero no modelado basado en agentes, per se. En los videojuegos, por ejemplo, cada oponente o cada personaje del juego que no está controlado por un jugador se programa utilizando el enfoque del sistema multiagente. Los chicos malos y los tiradores, por ejemplo, etc. Pero no hablaré de ello. Nos limitaremos al modelado basado en agentes. Pero, todo lo que diré aquí sobre el sistema multiagente es válido para todas las aplicaciones. En primer lugar, a menudo los agentes están especializados. Tienen una ubicación especial. Su entorno es un dominio especial puede ser 2D, puede ser 3D, o puede tener la topología que desee, por ejemplo, un grafo. Si le interesa la propagación de epidemias, no sólo le interesa dónde están las personas, porque se moverán. Lo que interesa es el grafo de la relación. Por ejemplo, cada agente, tiene una mayor probabilidad de infectar a sus compañeros de trabajo, a su familia, etc., así que lo que es el verdadero dominio especial aquí es un gráfico con la relación entre los agentes. Pero, realmente a menudo, están en 2D y 3D, y el ejemplo que le mostraré es el caso. El agente de una ubicación, podría moverse, por ejemplo, animales, peatones, puede moverse si tiene un modelo de tráfico, el agente se moverá y así sucesivamente. Y esta ubicación es realmente interesante, es realmente importante. Porque podría afectar a dos cosas. En primer lugar, a la conciencia de percepción de los agentes, del entorno. En lugar de verlo todo, sólo verá un poco de él. Y entonces la posición actual de un agente puede cambiar la red de interacción entre otros agentes. Así que dos pequeños ejemplos aquí. A la izquierda, así que aquí tenemos un entorno, paredes negras como una especie de laberinto. Y los agentes son los puntos negros. Y en verde, se ve la conciencia del entorno de cada agente. Así que aquí, le presento dos tipos de enfoque. El primer agente es consciente de su entorno inmediato con un radio. Por ejemplo, puede ser una señal de radio, y el agente puede percibir otra cosa que esté en el mismo radio o puede escuchar el ruido a su alrededor, etc. Pero podemos tener otros enfoques. Por ejemplo, en ese caso, el agente tiene una línea de visión. Tienen una dirección a la que apuntan y sólo pueden ver lo que tienen delante. Así que puede decidir como quiera cuál sería la conciencia de entorno del agente, qué percibirá exactamente. Y en muchos modelos interesantes eso cambiaría con su posición espacial. El otro parámetro que se ve afectado es la topología de interacción, porque aquí de nuevo, los agentes son puntos negros. Y si ve una línea azul entre dos puntos significa que esos dos agentes pueden interactuar. A la derecha, puede ver un ejemplo de una red de interacción agente en la que cada agente puede interactuar con los demás. Ese sería el caso, por ejemplo, del sistema de radio con el que cada agente puede decodificar o enviar un mensaje al otro. Y no sé, el teléfono o el correo electrónico donde todos pueden llegar a todas las personas. Pero también puede interactuar otra topología de interacción. Por ejemplo, puede ver que la interacción se produce sólo en los contactos. Así que cuando dos agentes están en contacto, pueden intercambiar algo. O puede tener, por ejemplo, la topología puede depender de la conciencia especial de cada agente. En ese ejemplo, por ejemplo, el agente puede comunicarse sólo si son conscientes de la misma región del espacio. Así, estos tres agentes pueden comunicarse pero no aunque demasiado lejos porque no ven la misma zona. Pero pueden utilizar al agente del medio para retransmitir. O estos tres agentes, estos cuatro agentes de aquí pueden comunicarse entre sí porque están bastante cerca y así sucesivamente. Así que tiene que decidir si están interactuando, ¿cuál es la topología? ¿Sólo es un contacto más corto, uno corto de nodos? Y así cambiaría mucho, la salida y la dinámica de su red. Así que aquí, hablaremos en el resto de esta conferencia sobre modelos basados en agentes. De nuevo, un modelo basado en agentes es el uso de un sistema multiagente para modelar un fenómeno natural. Y de nuevo modelamos el agente, las entidades individuales básicas, pero observamos un comportamiento complejo global, y ese es el comportamiento que queremos validar con la naturaleza que queremos comparar con experimentos. Ahora que defino lo que es un sistema multiagente, dedicaré el siguiente módulo a explicarles cómo suelen implementarse en un sistema informático. [MÚSICA]