[MÚSICA] Bienvenidos de nuevo al curso de la segunda semana sobre simulación y modelación de procesos naturales. En este décimo módulo hablaremos de estructuras de control específicas, que son los bucles. Pues bien, el primer bucle del que vamos a hablar es el bucle for. Pues bien, el bucle for es un bucle en el que haremos un bucle sobre cualquier tipo de secuencia ordenada. Así que, quizás podamos echar un vistazo a la sintaxis para intentar entender lo que hace este bucle for. Así que la sintaxis del bucle for empieza con una palabra clave for, un espacio, y luego el nombre de una variable, un espacio, la palabra clave in, luego una secuencia, y luego la señal de dos puntos. Después del símbolo de dos puntos tiene que ir a una nueva línea, indentar su código, las líneas siguientes, y poner sus sentencias aquí. Entonces, lo que se realiza es que esa variable tomará en orden cada valor de los objetos que están almacenados en secuencia. Y cada vez que tome un nuevo valor, ejecutará todas las sentencias. Así que aquí están todas las sentencias_1. Una vez que ha tomado todos los valores disponibles en secuencia, puede ejecutar la sentencia else, que es opcional. Así que, unas cuantas observaciones antes de pasar al ejemplo. De nuevo, al igual que para las sentencias if, la sangría es especialmente importante. Los objetos que se almacenan en la secuencia pueden ser de cualquier tipo. No es necesario que sean sólo números enteros, pueden ser una mezcla de cualquier cosa. Si desea fijarse sólo en números, la secuencia será más bien un rango. Por último, algo importante que puede hacer es salir de su bucle antes de haber recorrido todos los objetos de su secuencia con la sentencia break. Cuando llegue a una sentencia break, entonces saldrá del bucle. Veremos un ejemplo de esto ahora mismo. En este ejemplo de la derecha de la diapositiva, creamos una lista de animales que son gato, perro, vaca y pájaro. Y queremos iterar en esta lista y ver si en esta lista uno de los animales se llama vaca. Si un animal es una vaca entonces rompemos el bucle y salimos. Bien entonces, ¿qué ocurre? Empezamos creando la lista, y entramos en nuestro bucle for. Entonces, lo primero que ocurrirá es que x tomará el valor de gato. Después se probará si es vaca. No lo es, así que imprimirá que somos gato. Después volvemos al principio del bucle, porque esta secuencia no ha terminado. x tomará el valor perro, se probará. ¿Es igual a vaca? No lo es, por lo que perro se imprimirá en la pantalla. Entonces x tomará el valor vaca. Ahora como x es igual a vaca ahora, el bucle se romperá, y saldremos del bucle. Así que la sentencia else nunca se ejecutará. Hay otro tipo de bucle en Python, que es el bucle while. El bucle while es una sentencia que se ejecuta mientras se cumpla una condición. Así que la sintaxis es la siguiente. Tiene la palabra clave, while, un espacio, una determinada condición que puede ser verdadera o falsa, seguida de dos puntos. Después de los dos puntos, tiene una cierta cantidad de sentencias con sangría. Y si el bucle termina normalmente, tiene una sentencia else opcional que puede ejecutar. De acuerdo, así que de nuevo, algunas observaciones. Las sangrías son muy importantes. Y en cuanto al bucle for , podemos salir del bucle while en cualquier momento si llegamos a una sentencia break. En el ejemplo, donde el código está en el molino, lo que hacemos es que queremos hacer la suma de enteros en un array. Así que el usuario da un número de entrada, y entonces construiremos un rango hasta este número. Almacenaremos la suma total en una variable, que se llama tot. Entonces también tendremos un índice i, que se inicializa por 0. Entonces mientras nuestro índice i sea menor que la longitud del array de números que hemos creado, entonces sumaremos el contenido del índice i-ésimo del array a nuestro total. Entonces incrementaremos en 1, el índice, i. Y luego volveremos a la sentencia while. Si i sigue siendo menor que la longitud de la matriz, entonces seguiremos añadiendo los números. Una vez que i sea mayor o igual que la longitud de nuestra matriz, excederemos el bucle while e imprimiremos la sentencia else. Que dice que el bucle while se excedió normalmente. Así que ahora la salida para la entrada de un usuario dado. Así que el usuario dice que el número de entrada es cinco, la suma se realizará normalmente. Así que sumaremos cero, más uno, más dos, más tres, más cuatro. El resultado será diez. El bucle terminará normalmente porque no hay sentencia break. Y al final el programa emite la suma, que es diez. Bien, con esto terminamos el módulo sobre estructuras de control. Y en el próximo módulo hablaremos sobre funciones. Gracias por su atención. [MÚSICA]